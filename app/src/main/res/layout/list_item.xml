<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/tan_background"
    android:minHeight="@dimen/list_item_height"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <LinearLayout
        android:id="@+id/text_container"
        android:layout_width="match_parent"
        android:layout_height="@dimen/list_item_height"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_below="@id/description"
        android:orientation="vertical"
        android:paddingLeft="@dimen/padding">
        <!--Upper Text textview-->
        <TextView
            android:id="@+id/upper_text_view"
            android:layout_width="match_parent"
            android:layout_height="@dimen/list_item_text_height"
            android:layout_weight="1"
            android:gravity="bottom"
            android:textAppearance="?android:textAppearanceMedium"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            tools:text="UpperText" />
        <!--Lower text textview-->
        <TextView
            android:id="@+id/lower_text_view"
            android:layout_width="match_parent"
            android:layout_height="@dimen/list_item_text_height"
            android:layout_weight="1"
            android:gravity="top"
            android:textAppearance="?android:textAppearanceMedium"
            android:textColor="@android:color/white"
            tools:text="LowerTextView" />
    </LinearLayout>
    <!--Play button imageview-->
    <ImageView
        android:layout_width="@dimen/list_play_button_width"
        android:layout_height="@dimen/list_play_button_height"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="@dimen/padding"
        android:src="@drawable/ic" />
</RelativeLayout>